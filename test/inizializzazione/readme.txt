Questa e' la parte di inizializzazione del DB e contiene tutti i file JSON per inizializzare il DB
con qualche prodotto, ordine e fascia oraria cosi' non si perde tempo.

Per inserirli nel DB, con mongoDB Compass vai su "import file".